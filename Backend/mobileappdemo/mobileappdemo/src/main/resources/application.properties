spring.application.name=mobileappdemo

# Disable Docker Compose
spring.docker.compose.enabled=false

# Server Config
server.port=8082

# MySQL Database Config
spring.datasource.url=jdbc:mysql://localhost:3306/auth_db
spring.datasource.username=root
spring.datasource.password=Redpope2005.

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

# JPA Configuration (set to validate when using Flyway)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Email Config (Gmail SMTP example)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=isrealabefah2005@gmail.com
spring.mail.password=xdcg ziwv igdg xhda
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Paystack Configuration
paystack.secret.key=sk_test_your_paystack_secret_key_here
paystack.public.key=pk_test_your_paystack_public_key_here
paystack.api.url=https://api.paystack.co

# Exchange Rate API Configuration
exchange.rate.api.key=your_exchange_rate_api_key_here
exchange.rate.api.url=https://api.exchangerate-api.com/v4/latest/

# Redis Configuration (for caching exchange rates)
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.timeout=2000ms


